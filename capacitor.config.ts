import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.team.dashboard',
  appName: 'IT SOFTWARE RPG',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
